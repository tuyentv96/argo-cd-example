apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
spec:
  rules:
  - host: cafe.example.com
    http:
      paths:
        - path: /apple
          backend:
            serviceName: hello-app1
            servicePort: 80
