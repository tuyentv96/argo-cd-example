apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
spec:
  rules:
  - host: cafe.example.com
    http:
      paths:
        - path: /apple
          backend:
            service:
              name: hello-app1
              port:
                number: 80
