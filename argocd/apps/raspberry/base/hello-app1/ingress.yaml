apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-app1-ingress
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - hosts:
    - picluster.xyz
    secretName: hello-app1-tls
  rules:
  - host: hello-app1.picluster.xyz
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: hello-app1
              port:
                number: 80
